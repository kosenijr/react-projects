import React, { useState } from "react";
import styles from ".";

function Accordion() {
  return (
    <div className="accordion">
      <accordion />
    </div>
  );
}

export default Accordion;
